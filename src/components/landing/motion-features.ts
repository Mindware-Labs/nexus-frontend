/* Features de motion cargadas en diferido por MotionProvider (LazyMotion).
   domMax y no domAnimation: navbar y use-cases dependen de layoutId. */
import { domMax } from "motion/react";

export default domMax;
